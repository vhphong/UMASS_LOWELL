#ifndef COMMON_H
#define COMMON_H

typedef enum status{ FAILURE, SUCCESS } Status;
typedef enum boolean{ FALSE, TRUE } Boolean;
typedef enum largerString { LEFT=-1, SAME, RIGHT} LargerString;
typedef void* Item_ptr;

#endif
